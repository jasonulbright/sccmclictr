Maintenance recovery release. The application remains on **.NET Framework 4.8**;
there is no .NET 10 migration.

- Replaced the reconstructed automation DLL with Roger Zander's original public
  C# source, built as part of this repository. No precompiled automation DLL is
  downloaded from NuGet.
- Restored original WMI model/query behavior and retained narrow security fixes.
- Fixed installed-software metadata, query refresh/error handling, and the
  mandatory-update collection argument.
- Preserved signed installer/portable packaging and Roger's copyright attribution.
- Corrected the former black-box claim and acknowledged other community forks.
- Included the automation library's corresponding source and LGPL/GPL notices.

Validation: the application and all 14 plugins build; 12 recovery regression tests
and 2 unit tests pass. Manual workstation checks were completed, including a live
.NET update progressing through download, pending reboot, app-triggered restart,
and Installed status.

Limitations: additional domain-context checks, including remote log access, remain
unverified. Automated live parity tests were not completed. Bulk-install and
individual policy actions do not have separately recorded live results.

Downloads: use the Setup EXE for installation or extract the Portable ZIP.
First-party binaries and the installer are signed and timestamped; the vendored
NavigationPane control retains its upstream unsigned binary. Verify downloads
against checksums.txt. Both packages include library-source.
